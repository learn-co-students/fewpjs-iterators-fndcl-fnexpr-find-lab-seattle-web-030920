const testVar = {}

function testFunc() {
  return "hi"
}


function superbowlWin(array) {
  let year
array.find(object=> {object.result === "W"

if (object.result === "W") {year=object.year}

})
return year
}